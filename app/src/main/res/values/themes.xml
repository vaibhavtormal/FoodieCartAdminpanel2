<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/amazon_primary</item>
        <item name="colorPrimaryDark">@color/amazon_primary_dark</item>
        <item name="colorAccent">@color/textcolor</item>
        <item name="android:windowLightStatusBar">true</item> <!-- For dark status bar text -->
    </style>

</resources>